services:
 db:
   image: postgres:16
   environment:
     POSTGRES_USER: postgres
     POSTGRES_PASSWORD: postgres
     POSTGRES_DB: jkpgcity
   ports:
     - "5432:5432"
   volumes:
     - dbdata:/var/lib/postgresql/data
volumes:
 dbdata: